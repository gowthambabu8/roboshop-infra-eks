apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: roboshop
spec:
  selector:
    # labels are used for selector
    project: roboshop
    tier: backend
    component: payment
  ports:
    - protocol: TCP
      port: 8080 # service port
      targetPort: 8080 # container port
